# Export-MssqlToCsv PowerShell 模組規格說明

## 1. Split-SqlTableName 函數
功能：拆分 SQL 表名稱
- 輸入：接受各種格式的表名 (如 `[DB].[Schema].[Table]`, `Schema.Table`, `Table`)
- 輸出：返回一個包含以下屬性的物件：
  - DatabaseName：資料庫名
  - SchemaName：模式名
  - TableName：表名
  - FullTableName：完整格式化的表名

## 2. Export-MssqlToCsv 函數
功能：將 SQL Server 的資料匯出為 CSV 檔案

### 主要參數組合（三種模式）
1. 表格模式 (-Table)：
   - 直接匯出指定表格
   - 例：`Export-MssqlToCsv "server" "user" "pwd" "DB.Schema.Table"`
   - 會自動獲取表格欄位資訊並生成適當的 SQL 查詢

2. SQL 檔案模式 (-SQLPath)：
   - 從 SQL 檔案讀取查詢
   - 例：`Export-MssqlToCsv "server" "user" "pwd" -SQLPath "query.sql"`
   - 輸出檔名預設使用 SQL 檔案名稱（不含副檔名）

3. SQL 查詢模式 (-SQLQuery)：
   - 直接執行 SQL 查詢字串
   - 支援管道輸入
   - 例：`Export-MssqlToCsv "server" "user" "pwd" -SQLQuery "SELECT * FROM Table"`
   - 預設輸出檔名為 'QueryResult.csv'

### 重要功能選項

#### 1. 編碼設定
- -UTF8：無 BOM 的 UTF-8
- -UTF8BOM：有 BOM 的 UTF-8
- -Encoding：自定義編碼

#### 2. 輸出控制
- -Path：指定輸出路徑
- -OutToTemp：輸出到臨時目錄

### CSV 輸出格式規則（依照 SSMS 標準）
1. 基本規則：
   - 欄位間使用逗號分隔
   - 標題列不加引號
   - 預設不使用引號包覆
   - 需要時才使用雙引號包覆
   - 行尾使用 CRLF (\r\n)

2. 引號處理規則：
   - 當欄位包含以下字元時，使用雙引號包覆：
     - 逗號 (,)（不包括全形逗號）
     - 換行符 (LF/CRLF)
     - 雙引號 (")
     - 前後空白
     - TAB 字元
     - 純空白字串
   - 欄位中的雙引號用兩個雙引號表示 ("")
   - 單引號不需特殊處理
   - 連續多個引號時，每個引號都轉為兩個引號

3. 特殊值處理：
   - NULL：輸出為 NULL（不帶引號）
   - 空字串：輸出為空值（不帶引號）
   - 數值：直接輸出（不帶引號）
   - 日期時間：直接輸出（不帶引號）
   - 特殊字元（©®™）：直接輸出（不帶引號）
   - 表情符號：直接輸出（不帶引號）
   - 控制字元：輸出為空值（不帶引號）
   - 零字元：輸出為空格（不帶引號）
   - TAB：保留並用引號包覆
   - Unicode 空白字元：
     - 零寬度空格 (U+200B)：保留但不加引號
     - 不斷行空格 (U+00A0)：保留但不加引號
     - 全形空格 (U+3000)：保留但不加引號
     - 中日韓空格 (U+2002)：保留但不加引號
     - 窄空格 (U+2009)：保留但不加引號
     - 數學空格 (U+205F)：保留但不加引號
   - 分號、豎線：直接輸出（不帶引號）
   - 全形標點：直接輸出（不帶引號）
   - \r\n 字面值：直接輸出字面值（不轉換為實際換行）

4. 換行處理說明：
   - 當欄位中包含實際換行符時（如 LF 或 CRLF）：
     - SSMS 會保留實際的換行符（不轉換為字面值）
     - 整個欄位會被雙引號包覆
     - 在 CSV 檔案中會呈現為真實的多行文字
     - 這符合 RFC 4180 CSV 標準
     - 注意：這會導致 CSV 檔案的實際行數多於資料列數
   - 字面值的換行符（如字串中的 "\r\n"）：
     - 會直接輸出為字面值，不會轉換為實際換行
     - 如果欄位中同時包含逗號等需要引號的字元，則整個欄位會被引號包覆

5. 值對照表：

   |DB值|CSV輸出格式|說明|
   |---|---|---|
   |DBNull.Value|NULL|不加引號|
   |String.Empty||不加引號|
   |包含逗號的文字|"Hello,World"|加雙引號|
   |包含換行的文字|"Line1\nLine2"|加雙引號|
   |包含引號的文字|"Hello""Quote"|雙引號加倍|
   |前後有空白|"   Hello   "|加雙引號|
   |純數值|1234.56|不加引號|
   |日期時間|2025-03-08 14:23:41.927|不加引號|
   |特殊符號|©®™|不加引號|
   |中文字|中文測試|不加引號|
   |表情符號|🌟🎉|不加引號|
   |TAB字元|"   "|加雙引號|
   |控制字元||輸出空值|
   |零字元| |輸出空格|
   |反斜線|\\|不加引號|
   |多重引號|"""Hello""World"""|每個引號都加倍|
   |格式化數值|"$1,234.56"|因逗號加引號|
   |格式化日期|"2024/03/08, 14:30"|因逗號加引號|
   |URL含逗號|"https://example.com,index.html"|因逗號加引號|
   |XML含逗號|"<tag>,</tag>"|因逗號加引號|
   |全形標點|，。：|不加引號|
